Choose a ticket class:
%select
  - on "change", @chosenTicket
  - each @tickets, ->
    %option= @name

%button Clear
  - on "click", @resetTicket

%p
  - observing @chosenTicket, ->
    - if @price
      You have chosen
      %b= @name
      %span
        $
        = @price
